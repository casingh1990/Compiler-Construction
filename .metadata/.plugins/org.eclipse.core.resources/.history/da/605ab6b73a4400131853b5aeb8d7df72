public class Compiler {
	
	public static void main(String[] args) {
		String filename = "hello_world.pas";
		Scanner tk_search = new Scanner(filename);
		tk_search.findTokens();
		Parser parser = new Parser(tk_search.getTokens());
		parser.parse();
	}

}